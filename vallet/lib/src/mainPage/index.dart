part of vallet;

class Main extends StatefulWidget {
  final String title;
  final String type;
  Main({
    Key key,
    this.title,
    this.type,
  }) : super(key: key);

  @override
  MainState createState() => MainState();
}
